
8.
클래스 변수: width, height
인스턴스 변수: kind, num
지역 변수: k, n, card

9.
weapon, armor
armorUp(), weaponUp()
모든 병사가 동일한 방어력과 공격력을 가지기 때문이다.
또한 이 클래스 변수 값을 변화시키는 매소드도 스태틱이어야 한다.

10.
b: 생성자는 객체 초기화를 위한 매소드이다. 객체 생성은 new가 한다.
e: 다른 인자 조합을 넣는 생성자를 여러 개 정의할 수 있다.

11.
b: 클래스 맴버에는 사용할 수 없다.

12.
c:
d:
오버로딩은 다른 메소드를 만드는 것이다.
메서드 이름이 같고 매개변수가 다르면 된다.


13.
b, c, d

14.
c: 초기화 후 생성자가 수행된다.
e: 클래스 맴버는 스태틱이므로, 파일이 적재시에 수행된다.

15.
a


16.
a: 인스턴스 변수는 자동으로 초기화되지만, 지역변수는 별도의 초기화 작업이 필요하다.
e: 힙영역에는 인스턴스가 생성되며, 지역변수는 호출스택에 생성된다.

17.
b: 나머지 메서드는 대기 상태이다.

18.
A: 스태틱 변수 초기화에 인스턴스변수를 사용할 수 없다.
B: 스태틱 메서드에서는 인스턴스변수를 사용할 수 없다.
D: 스태틱 매서드에서는 인스턴스 메서드를 사용할 수 없다.

19.
ABC123
After change:ABC123

:change매서드가 종료되면 매개변수이자 지역변수인 str도 사라진다.
