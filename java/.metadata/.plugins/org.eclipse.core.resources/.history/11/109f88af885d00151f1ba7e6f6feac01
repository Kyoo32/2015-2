package station;

import client.Client;
import main.Main;

public class TicketBox {
	TrainStation ts = Main.traS;
	public void ticketing(Client c){
		c.ticketingTimeSpent();
		System.out.println("5" + ts);
		ts.enqueue(c);
	}

}
